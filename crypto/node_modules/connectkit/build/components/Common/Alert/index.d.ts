/// <reference types="react" />
import { AlertProps } from './types';
declare const Alert: {
    ({ children, error, icon }: AlertProps): JSX.Element;
    displayName: string;
};
export default Alert;
