import { Connector } from 'wagmi';
import { WalletConfigProps } from '../constants/walletConfigs';
type WalletProps = {
    id: string;
    connector: Connector<any, any>;
    isInstalled?: boolean;
    createUri?: (uri: string) => string;
} & WalletConfigProps;
export declare const useWallet: (id: string, name?: string) => WalletProps | null;
export declare const useWallets: () => WalletProps[];
export {};
