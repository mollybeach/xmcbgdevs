import { ReactNode } from 'react';
import { Connector } from 'wagmi';
interface ConnectorInfo {
    id: string;
    name?: string;
    icon?: ReactNode;
    connector?: Connector;
}
export declare const getConnectorInfo: (id: string, injectedUUID?: string) => ConnectorInfo | null;
export {};
